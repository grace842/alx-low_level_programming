#include <stdio.h>
/**
 * main - Entry point
 * Description: 'prints numbers'
 * Return: always 0
 */
int main(void)
{
	int i;

	for (int i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
