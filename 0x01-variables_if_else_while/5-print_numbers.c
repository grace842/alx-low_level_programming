#include <stdio.h>
/**
 * main - Entry point
 * Description: 'prints numbers'
 * Return: always 0
 */
int main(void)
{
	int n;

	for (int n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	printf("\n");
	return (0);
}
